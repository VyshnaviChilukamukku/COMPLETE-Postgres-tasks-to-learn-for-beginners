PG user login
psql -U iri_jira_latest_qa -d iri_jira_latest    =   'jirq_876'

List of DB running in postgres

$psql
SELECT datname FROM pg_database;

To view all of the defined databases on the server you can use the 
\list meta-command or its shortcut \l.

Listing Tables
\dt
